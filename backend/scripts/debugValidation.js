const express=require('express');const bodyParser=require('body-parser');const adminRoutes=require('../routes/adminRoutes');process.env.NODE_ENV='test';const app=express();app.use(bodyParser.json());app.use('/api/admin',adminRoutes);const request=require('supertest');(async()=>{const r=await request(app).get('/api/admin/users?sortBy=hack_column');console.log('Status',r.status);console.log(r.body);const r2=await request(app).get('/api/admin/declarations/status-audit?from=2025-12-31&to=2025-01-01');console.log('Audit Status',r2.status);console.log(r2.body);})();