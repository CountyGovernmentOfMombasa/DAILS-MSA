# Database Configuration
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=employee_declarations
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your_very_secure_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Mailer Configuration (SMTP)
# MAIL_PORT 465 usually implies implicit TLS (secure=true)
# Set MAIL_SECURE=true for port 465 or false for STARTTLS on 587
# MAIL_TLS_VERIFY_PEER=false will skip TLS peer verification (NOT recommended)
MAIL_MAILER=smtp
MAIL_HOST=mail.mombasa.go.ke
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USERNAME=dials@mombasa.go.ke
MAIL_PASSWORD=@Mombasa9009
MAIL_FROM_ADDR=dials@mombasa.go.ke
MAIL_FROM_NAME='CGM-DIALS'
MAIL_REPLYTO_ADDR=dials@mombasa.go.ke
MAIL_REPLYTO_NAME='CGM-DIALS'
MAIL_TLS_VERIFY_PEER=true

# SMS (Tolclin)
SMS_ENABLED=true
TOLCLIN_BASE_URL=https://tolclin.com/tolclin/sms/BulkSms
TOLCLIN_CLIENT_ID=254
TOLCLIN_SENDER_ID=COUNTY-MSA
TOLCLIN_CALLBACK_URL=
